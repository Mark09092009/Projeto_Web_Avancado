"""
Teste(s) para o app `servicos`.

Este arquivo deve conter classes de teste que herdem de
`django.test.TestCase`. Coloque aqui os testes unitários e de integração
relacionados à funcionalidade de listagem de serviços, rotas e templates
do app `servicos`.

Exemplo mínimo:

	from django.urls import reverse

	class ServicosViewTests(TestCase):
		def test_lista_servicos_status_200(self):
			resp = self.client.get(reverse('lista_servicos'))
			self.assertEqual(resp.status_code, 200)

"""

from django.test import TestCase

# Arquivo preparado para receber testes do app `servicos`.
