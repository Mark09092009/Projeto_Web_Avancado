"""
Testes para o app `core`.

Coloque aqui testes relacionados a views públicas (home, sobre) e ao fluxo
de autenticação/registro que residem no app `core`.

Exemplo:

	from django.urls import reverse

	class CoreViewTests(TestCase):
		def test_home_status_200(self):
			resp = self.client.get(reverse('home'))
			self.assertEqual(resp.status_code, 200)

"""

from django.test import TestCase

# Arquivo preparado para receber testes do app `core`.
