"""
Modelos do app `core`.

Este arquivo é o local padrão para modelos ligados a funcionalidades
centrais do projeto (por exemplo, perfis estendidos de usuário, logs
ou entidades que não se encaixam nos apps específicos). Hoje não há
modelos definidos aqui; o arquivo existe para facilitar a extensão
do projeto no futuro.

Se for necessário adicionar modelos, lembre-se de criar e aplicar
migrations com `python manage.py makemigrations` e `migrate`.
"""

from django.db import models

# Nenhum modelo definido no momento. Arquivo mantido como placeholder.
